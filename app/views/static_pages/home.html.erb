<% if logged_in? %>
  <div class="row">
      <aside class="col-md-4">
          <section class="user_info">
              <%= render 'user_info' %>
          </section>
          <section class="micropost_form">
              <%= 'shared/micropost_form' %>
          </section>
      </aside>
  </div>
<% else %>
  <div class="centet jumbotron">
      <h1>Welcim to the Microposts</h1>
      <%= link_to "Sign up now!", singup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
